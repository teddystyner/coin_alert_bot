name: Test Every 2 Minutes

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print UTC & KST
        run: |
          echo "✅ UTC Time: $(date -u)"
          echo "✅ KST Time: $(TZ=Asia/Seoul date)"
